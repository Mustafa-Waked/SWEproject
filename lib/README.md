# External libraries

Place these JARs in this folder before building in Eclipse (or add them to your classpath):

| File | Download |
|------|----------|
| `mysql-connector-j.jar` | [MySQL Connector/J](https://dev.mysql.com/downloads/connector/j/) |

The Eclipse `.classpath` references `lib/mysql-connector-j.jar` with a project-relative path.
